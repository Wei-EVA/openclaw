diff --git a/dist/providers/anthropic.js b/dist/providers/anthropic.js
index e94eff863c3227604cdefcd9ff2087bda03be979..29ec4e4b96ff0bbb2fc76c72556b11e665eb59cc 100644
--- a/dist/providers/anthropic.js
+++ b/dist/providers/anthropic.js
@@ -466,7 +466,7 @@ function convertMessages(messages, model, isOAuthToken, cacheControl) {
                         };
                     }
                 });
-                let filteredBlocks = !model?.input.includes("image") ? blocks.filter((b) => b.type !== "image") : blocks;
+                let filteredBlocks = !model?.input?.includes("image") ? blocks.filter((b) => b.type !== "image") : blocks;
                 filteredBlocks = filteredBlocks.filter((b) => {
                     if (b.type === "text") {
                         return b.text.trim().length > 0;
diff --git a/dist/providers/google-shared.js b/dist/providers/google-shared.js
index d272b57da187bd4d7708fe49b6e553892678b52d..9287c4ca34802921dc508d9f0c988ea24c256327 100644
--- a/dist/providers/google-shared.js
+++ b/dist/providers/google-shared.js
@@ -90,7 +90,7 @@ export function convertMessages(model, context) {
                         };
                     }
                 });
-                const filteredParts = !model.input.includes("image") ? parts.filter((p) => p.text !== undefined) : parts;
+                const filteredParts = !model.input?.includes("image") ? parts.filter((p) => p.text !== undefined) : parts;
                 if (filteredParts.length === 0)
                     continue;
                 contents.push({
@@ -173,7 +173,7 @@ export function convertMessages(model, context) {
             // Extract text and image content
             const textContent = msg.content.filter((c) => c.type === "text");
             const textResult = textContent.map((c) => c.text).join("\n");
-            const imageContent = model.input.includes("image")
+            const imageContent = model.input?.includes("image")
                 ? msg.content.filter((c) => c.type === "image")
                 : [];
             const hasText = textResult.length > 0;
diff --git a/dist/providers/openai-completions.js b/dist/providers/openai-completions.js
index 3b57cd261f37b7075445866a2b7b68a77c0b09e6..121b3dcfea533351a73c9c049613b9e194c05870 100644
--- a/dist/providers/openai-completions.js
+++ b/dist/providers/openai-completions.js
@@ -479,7 +479,7 @@ export function convertMessages(model, context, compat) {
                         };
                     }
                 });
-                const filteredContent = !model.input.includes("image")
+                const filteredContent = !model.input?.includes("image")
                     ? content.filter((c) => c.type !== "image_url")
                     : content;
                 if (filteredContent.length === 0)
@@ -596,7 +596,7 @@ export function convertMessages(model, context, compat) {
                     toolResultMsg.name = toolMsg.toolName;
                 }
                 params.push(toolResultMsg);
-                if (hasImages && model.input.includes("image")) {
+                if (hasImages && model.input?.includes("image")) {
                     for (const block of toolMsg.content) {
                         if (block.type === "image") {
                             imageBlocks.push({
diff --git a/dist/providers/openai-responses-shared.js b/dist/providers/openai-responses-shared.js
index e2c148c8ee699472571be30f98e96a200f42517f..7abec3aa3c200b0baa1a987a48554ae2ec42238e 100644
--- a/dist/providers/openai-responses-shared.js
+++ b/dist/providers/openai-responses-shared.js
@@ -74,7 +74,7 @@ export function convertResponsesMessages(model, context, allowedToolCallProvider
                         image_url: `data:${item.mimeType};base64,${item.data}`,
                     };
                 });
-                const filteredContent = !model.input.includes("image")
+                const filteredContent = !model.input?.includes("image")
                     ? content.filter((c) => c.type !== "input_image")
                     : content;
                 if (filteredContent.length === 0)
@@ -155,7 +155,7 @@ export function convertResponsesMessages(model, context, allowedToolCallProvider
                 output: sanitizeSurrogates(hasText ? textResult : "(see attached image)"),
             });
             // If there are images and model supports them, send a follow-up user message with images
-            if (hasImages && model.input.includes("image")) {
+            if (hasImages && model.input?.includes("image")) {
                 const contentParts = [];
                 // Add text prefix
                 contentParts.push({
